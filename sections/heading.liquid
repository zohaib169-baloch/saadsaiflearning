<h1 class="text">{{ section.settings.text }}</h1>
{% schema %}
{
    "name": "Heading",
    "settings": [
        {
            "type": "text",
            "label": "Text",
            "id": "text",
            "default":"Hello Starter Theme"
        }
        ],
        "presets": [
            {
                "name": "Heading"
            }
        ]
}
{% endschema %}